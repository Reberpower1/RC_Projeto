#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define BUFLEN 512	// Tamanho do buffer
#define PORT 9876	// Porto para recepção das mensagens

void erro(char *s) {
	perror(s);
	exit(1);
}

int main(){
    

    return 0;
}